public class ServerMain {
    public static void main(String[] args) {
        chatServer server = new chatServer();
        server.startServer(); // Starts the server and begins accepting client connections
    }
}